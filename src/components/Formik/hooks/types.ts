export type UseIsSubmitDisabledProps = {
  isDisabled?: boolean;
  isLoading?: boolean;
  skipDirtyCheck?: boolean;
};

export type UseFormWrapperProps = {
  isDisabled?: boolean;
  isLoading?: boolean;
  skipDirtyCheck?: boolean;
};
